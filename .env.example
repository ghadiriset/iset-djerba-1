# Copier ce fichier en .env et remplir les valeurs

# Secret de session (changer en production !)
SESSION_SECRET=change-moi-en-production-abc123xyz

# Port du serveur (optionnel, défaut: 3000)
PORT=3000
